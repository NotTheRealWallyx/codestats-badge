{
  "description": "A dynamic SVG badge generator for Code::Stats profiles, supporting themes and customization.",
  "type": "module",
  "license": "GPL-3.0",
  "dependencies": {
    "axios": "^1.10.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^9.30.1",
    "nodemon": "^3.1.0",
    "prettier": "^3.6.2",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  }
}
